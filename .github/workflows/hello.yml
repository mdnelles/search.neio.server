name: Hello Script
on: [push, workflow_dispatch]
jobs:
   build:
      # The type of runner that the job will run on
      runs-on: self-hosted
      steps:
         - name: Say Hello
           run: echo " -----Hello workflow is initiated"
